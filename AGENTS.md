# rize

AI Agents sandbox

## Important

- For any changes,always test and verify the image is working, via `make build` and `make exec`.

## Commands

- `make build` - Build the project
- `make exec` - One off shell command
- `make install` - Install the project to user local path

## Testing

1. `make build`
2. `make exec` to run commands in the container
